# Backlog

Priorities are the orchestrator's triage (serving done-criteria; #2 generate-process-map = top). Done-criteria #3 requires every **P0 and P1** item done. P2 items are tracked but not required to satisfy the done-criteria.

| id | source | priority | status | acceptance |
| --- | --- | --- | --- | --- |
| B1 | RES-A | P0 | done | `DependencyMap` uses `useNodesState`/`useEdgesState`; dragging a node persists; running analysis after generate does not reposition/re-fit the map (layout re-seeds only on structural id change); highlight/attribute updates preserve node positions; `npm run build` clean. — VERIFIED: build green, phase10Check PASS, diff scoped to DependencyMap.tsx (+57/-16). |
| B2 | ARCH-2, RES-D | P0 | done | Prepared reveal is guarded: clicking Generate then immediately switching project and back renders the K-12 map with no stuck overlay and no blank map; rapid double-click never leaves a blank map; the ingest overlay and graph reveal run off one clock (no blank-overlay/early-pop). `ingesting` resets on `selectProject`/unmount. — VERIFIED: build green; `activeProjectIdRef` guard at prepared timer (1515) + live apply (1546); zero-network prepared path preserved; INGEST_DURATION documented as single clock (1160<1650). Note: in-flight live extract dropped on switch-away (acceptable). |
| B3 | ARCH-1 | P0 | done | A shared `validateRefs` (new `lib/graph.ts`) is used by extract and analyze; POST `/api/gemini/analyze` with a relationship to a non-existent entity returns 422, not a degraded 200. — VERIFIED: build green, validateGraph+phase10 PASS, lib/graph.ts created, 422 added to analyze before buildPrompt. |
| B4 | RES-F | P1 | done | Live extraction uses Gemini structured output (`responseMimeType:"application/json"` + `responseSchema` mirroring `Graph`) with `stripFences`+`validateRefs` fallback; `lib/phase5Check.ts` and `lib/phase6Check.ts` pass against a running dev server; prepared path still does zero network. — VERIFIED: build green, validateGraph+phase10 PASS. `GRAPH_RESPONSE_SCHEMA` added to lib/graph.ts; `generateWithFallback` takes optional `generationConfig` (byte-for-byte unchanged when omitted — analyze route untouched); extract passes schema only. Prepared path still zero-network (schema only affects live extract). (Live phase5/phase6 deferred — needs running server + key.) |
| B5 | ARCH-4 | P1 | done | Failure classes map to status codes: overload/timeout → 503 `retryable:true`; malformed model JSON → 502; quota exhausted → 429. A forced malformed response yields 502 and the map error card shows a clear "model output could not be parsed" message with working Retry. — VERIFIED: build green; `geminiErrorResponse`+`MalformedModelOutputError` in lib/gemini.ts; both routes' catch-alls use it; parse wrapped in runExtraction/callGemini. (Live forced-malformed test deferred — needs running server.) |
| B6 | UX-1, UX-2, UX-3, UX-5 | P1 | done | Generate-map flow is intuitive: (UX-2) no-selection label reads "from all N sessions" vs "from N selected" with a Select-all affordance; (UX-3) step numbering is consistent across Sessions header/footer and FlowStepper (single source of truth); (UX-1) generating shows a visible confirmation explaining the view change; (UX-5) a prominent "Run analysis" CTA appears in the primary content area once a map exists. Build clean. — VERIFIED: build green, validateGraph+phase10 PASS. UX-3: new module-level `FLOW_STEP_DEFS` + `STEP_NO` single source — FlowStepper and Sessions "Step N" labels both derive numbering from it (no drift). UX-2: label now "from all N sessions" / "from the open session" / "from N selected"; added Select all/Clear toggle in list header. UX-1: action bar shows "Building the map — opening Map & analysis…" while generating (view already flips to Map on click). UX-5: accent CTA bar between the map header and canvas ("Map ready — run analysis…" + Run analysis button) shown once a map exists and no insight yet. |
| B7 | ARCH-3 | P1 | done | Analyze route no longer clobbers a global `seed/latest-analysis.json` for user projects (writes scoped or gated); the secondary review is appended only when the detected crossover matches the seed scenario; two user projects analyzed back-to-back keep independent, correct analyses. — VERIFIED: build green, validateGraph+phase10 PASS. Route now takes `projectId`; global write gated to K-12/prepared (no projectId or `K12_PROJECT_ID`); secondary appended only when graph contains its `entitiesInvolved` (proc-award/proc-payment) — unrelated user projects get `[hero]` only. Client `analyzeGraph` sends `projectId: project.id`. Seed K-12 graph still yields `[hero, secondary]` + latest-analysis.json (phase6 path unchanged). |
| B8 | ARCH-6 | P1 | done | Session fetch is scoped: switching to a project issues `GET /api/sessions?projectId=…` (or the global-fetch tradeoff is explicitly documented in code); no behavior regression in the Sessions list. — VERIFIED (documented-tradeoff branch): the shell's `refreshSaved` deliberately fetches ALL sessions because the Processes overview + ProjectSwitcher render a session count for every project at once (`overviews` maps all projects through `sessionsForProject`); per-project scoping would zero out non-active projects' counts — a regression. Route still supports `?projectId=` for scoped callers. Tradeoff now documented in code at `refreshSaved`. No behavior change; build green. |
| ARCH-5 | architect-reviewer | P1 | done | Human-approved ("Do the refactor now"). AtlasApp.tsx split: presentational + helper layer moved verbatim to `components/atlas/parts.tsx` (types, atoms, SessionsView, FlowStepper, ProjectSwitcher, ProcessesView, modals, plus new TopBar/MapView wrappers); 3 reveal structures collapsed to one `revealed` state `Map<id,{map,analysis}>` (`markMapRevealed`/`markAnalysisRevealed`/`clearRevealed`). — VERIFIED: AtlasApp.tsx 1968 → 599 lines (< 600); build green; validateGraph 17/28 + phase10Check PASS. Byte-identical programmatic split (Node fs slice) chosen over hand-transcription to eliminate CSS-string typo risk on demo-critical code. Reveal-as-state safe: `revealedRef` was never read same-tick as written (hydrateDisplay reads only from selectProject after separate renders). No behavior change to generate/switch/analyze beats. |
| P2-batch | architect-reviewer, ux-reviewer | P2 | deferred | ARCH-7, ARCH-8, ARCH-9, UX-4, UX-6, UX-7, UX-8, UX-9, UX-10, UX-11, UX-12 — tracked in recommendations.md; not required by done-criteria #3. Revisit after P0/P1 are green if time allows. |
