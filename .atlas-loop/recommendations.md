| id | source | area | severity | problem | recommendation | acceptance | status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| ARCH-1 | architect-reviewer | generate-process-map / data integrity | P0 | `analyze/route.ts` only checks `entities`/`relationships` are arrays — it never validates relationship source/target ids resolve. A persisted graph with a dangling ref re-analyzed makes `findCrossovers`/`describeFlows` silently drop unresolved ids and the hero crossover can vanish. | Add `validateRefs` (shared from a new `lib/graph.ts`) to `analyze/route.ts` before `buildPrompt`, returning 422 on failure; extract and analyze use one implementation. | POST `/api/gemini/analyze` with a graph containing a relationship to a non-existent entity returns 422, not a 200 with a degraded insight. | done (B3) |
| ARCH-2 | architect-reviewer | generate-process-map reliability | P0 | Prepared K-12 reveal in `onGenerateMap` uses a fixed `setTimeout(INGEST_DURATION=1650ms)`. Switching project mid-ingest (`selectProject`→`clearTimers`) drops the reveal with `ingesting` already reset, leaving "No process map yet". | Guard the reveal: in the timer callback verify `activeProjectId` is still the project that started it before `setShownGraph`; on `selectProject`/unmount also `setIngesting(false)`. | Click Generate, immediately switch project and back: the K-12 map renders correctly with no stuck overlay; rapid double-click never leaves a blank map. | done (B2) |
| ARCH-3 | architect-reviewer | API design / consistency | P1 | `analyze/route.ts` writes global `seed/latest-analysis.json` on EVERY call including user projects, creating a clobber race when two projects analyze concurrently, and hard-wires the secondary review to the K-12 hero for unrelated workspaces. | Stop writing global `latest-*.json` from request handlers (or gate behind a debug flag). Append the secondary review only when the detected crossover matches the seed scenario. Persist user-project analysis solely through the projects store. | Analyze two different user projects back-to-back; each project's persisted analysis is correct and independent; no shared `latest-analysis.json` write races. | done (B7) |
| ARCH-4 | architect-reviewer | error handling / status codes | P1 | `generateWithFallback` throws a generic `Error` surfaced verbatim as HTTP 500 in both routes; a `JSON.parse(stripFences(raw))` failure also becomes an opaque 500. No retry-after / 503 distinction for the client. | Map failure classes to status codes: timeout/overload → 503 `retryable:true`; JSON parse failure → 502 ("model returned malformed output"); quota exhausted → 429. Wrap `JSON.parse` with a typed error. | Force a malformed model response: extract returns 502 with a clear message; the map error card shows "model output could not be parsed", and Retry works. | done (B5) |
| ARCH-5 | architect-reviewer | state management / coupling | P1 | `AtlasApp.tsx` is a ~1900-line client component with ~20 state slices plus three parallel reveal structures (`revealedRef`/`revealedMap`/`revealedAnalysis`) kept in sync manually — top maintainability risk and likely source of reveal desync. | Extract a `useProjectPipeline` hook owning pipeline state + actions; split `SessionsView`/`FlowStepper`/modals into files; collapse the three reveal structures into one `Map<projectId,{map,analysis}>`. | `AtlasApp.tsx` drops below ~600 lines; reveal state in one structure; build clean; generate/switch/analyze behave identically. | done (human-approved refactor) — presentational + helper layer split into `components/atlas/parts.tsx` (types, atoms, SessionsView, FlowStepper, ProjectSwitcher, ProcessesView, modals, TopBar, MapView); reveal collapsed to a single `revealed` state `Map<id,{map,analysis}>` with `markMapRevealed`/`markAnalysisRevealed`/`clearRevealed`. AtlasApp.tsx 1968 → 599 lines. Build green; validateGraph 17/28 + phase10Check PASS. |
| ARCH-6 | architect-reviewer | scaling to multiple workspaces | P1 | `refreshSaved` fetches `GET /api/sessions` with no `projectId`, loading every session for every workspace on every mount/interview-return; the API supports `?projectId=` but the client never uses it. | Fetch `?projectId=${activeProjectId}` and refetch on switch, or document the global-fetch tradeoff and keep memoized bucketing. | Switching to a project issues a scoped `GET /api/sessions?projectId=…`, or the global-fetch decision is documented. | done (B8, documented tradeoff) |
| ARCH-7 | architect-reviewer | error handling / UX | P2 | Best-effort `fetch(...).catch(()=>{})` in `persistProject`/`deleteProject`/`deleteSession` swallow failures with no feedback — silent data loss masked as success. | Keep optimistic UI but surface a non-blocking toast on persistence/delete failure. | Simulate a 500 from `POST /api/projects`; the user sees a recoverable warning rather than silent data loss. | open |
| ARCH-8 | architect-reviewer | generate-process-map / liveMode coupling | P2 | Prepared-vs-live branch keyed on `project.id === K12 && !liveMode` in three places; toggling `liveMode` ON for K-12 mid-demo switches to a real Gemini call that can fail. | Centralize in one `pipelineMode(project, liveMode)` helper used by generate and analyze; disable/annotate the Live toggle when the active project has no prepared fallback. | One helper governs prepared/live selection; toggling Live on a user project is visibly a no-op or labeled; K-12 prepared path provably zero-network. | open |
| ARCH-9 | architect-reviewer | concurrency / data store | P2 | Per-file writes are atomic but routes do read-modify-write of sibling files with no locking; acceptable for single-presenter demo. | Document the single-writer assumption in `projectStore.ts`/`sessionStore.ts`, or serialize writes through a tiny queue. | Comment added stating single-writer assumption; no corruption under sequential demo usage. | open |
| RES-A | researcher | generate-process-map / React Flow | P0 | `DependencyMap` renders `nodesDraggable` but derives nodes purely from props via `useMemo` with no `useNodesState`/`onNodesChange` — drag is dropped on next render; every prop-identity change re-runs dagre and re-fits, so analysis-time `entityAttributes` merge makes the map visibly jump. | Switch to `useNodesState`/`useEdgesState`; re-seed layout only on structural id change; apply highlight/attribute updates by mapping existing nodes (preserve positions); `fitView` after commit. | Dragging a node persists; running analysis after generating does not reposition/re-fit the map; build clean. | done (B1) |
| RES-D | researcher | generate-process-map / ingest timing | P1 | Prepared path `setTimeout(INGEST_DURATION=1650)` and `IngestOverlay`'s own ~1160ms internal timers are independent and can desync — overlay finishes blank or graph pops early. | Collapse to a single clock: `IngestOverlay` fires `onDone` after its last line and drives the prepared reveal, or confirm/align the durations. Keep ≤ ~1.7s; don't change `runPrepared` beats. | The ingest overlay and graph reveal are driven by one clock; no blank-overlay or early-pop flicker across repeated runs. | done (B2) |
| RES-F | researcher | generate-process-map / live extraction | P1 | Live extraction does `JSON.parse(stripFences(raw))` with no schema enforcement; the model is only asked in the prompt to return JSON — non-deterministic failures on the live path. | Add Gemini structured output (`responseMimeType:"application/json"` + `responseSchema` mirroring `Graph`) in `lib/gemini.ts`/extract route; keep `stripFences` + `validateRefs` as fallback. Server-only. | Live extract returns schema-valid JSON across repeated runs; `phase5Check`/`phase6Check` pass; prepared path unaffected (zero network). | done (B4) |
| UX-1 | ux-reviewer | generate-process-map | P1 | The generate-map action lives in a Sessions footer, but clicking it silently navigates to the Map view with no in-place feedback, so the user doesn't connect the click with the screen change. | Show a brief inline confirmation/toast in Sessions ("Map generated — opening Map & analysis") or label the action "Generate process map →" so the view change is expected. | Click Generate from Sessions; a visible confirmation appears and it is clear why the screen changed to Map. | done (B6) |
| UX-2 | ux-reviewer | generate-process-map | P1 | `targetsForMap = selected.length>0 ? selected : open ? [open] : sessions` — with nothing checked, Generate silently uses ALL sessions; the label "from N sessions" doesn't reveal that N = everything by default. | When no checkboxes are ticked show "from all N sessions" (vs "from N selected sessions") and add "Select all". Consider defaulting all sessions checked on entry. | With zero checkboxes ticked, the label reads "from all N sessions"; ticking any switches it to "from N selected." | done (B6) |
| UX-3 | ux-reviewer | generate-process-map | P1 | Three different "Step N" numbering schemes describe the same flow (FlowStepper, Sessions header "Step 1", Sessions footer "Step 2"), undercutting the stepper as single source of truth. | Pick one canonical numbering driven by the FlowStepper's `currentStep`, or drop the per-view "Step N" microcopy. Ensure the stepper's active step matches what the view asks. | Step numbers in Sessions header, Sessions footer, and FlowStepper are mutually consistent for the same state. | done (B6) |
| UX-4 | ux-reviewer | generate-process-map | P2 | After a successful prepared generate, there is no success confirmation — the overlay plays then the graph just appears; the moment of "done" is ambiguous. | Add a brief success toast/pulse when the graph renders ("Process map generated — run analysis next"), consistent with the accent visual language. | Generating a map produces a transient success confirmation that names the next action (Run analysis). | open |
| UX-5 | ux-reviewer | generate-process-map | P1 | Run-analysis's only trigger lives inside the right-hand Insight Panel as a small dashed card; new users on a fresh map may not look to the far-right panel for the next action. | Surface a primary "Run analysis" CTA on/near the Dependency Map header once a map exists; the FlowStepper "Insight" step should also be a runnable CTA. | After generating a map, a prominent Run-analysis CTA is visible in the primary content area without scanning the side panel. | done (B6) |
| UX-6 | ux-reviewer | loading/error feedback | P2 | In live mode the cinematic ingest overlay plays regardless of fetch state and could imply completion before the network returns. | Drive overlay dismissal off the actual fetch resolution; consider a determinate "Extracting…" state for live vs the scripted reveal for prepared. | In live mode with a slow network, the overlay stays until the fetch resolves and never shows "done" prematurely. | open |
| UX-7 | ux-reviewer | empty/status states | P2 | The "No process map yet" map empty state tells the user to go to Sessions but has no button to do it. | Add a primary "Go to Sessions" button reusing the existing `onGoToSessions` handler. | The Map empty state contains a clickable control that navigates to Sessions. | open |
| UX-8 | ux-reviewer | multi-project nav | P2 | The "MAY BE OUT OF DATE" staleness badge appears only on the Map header; deleting a session happens in Sessions, where there is no stale feedback. | Surface an inline "Map may be out of date — regenerate" notice in the Sessions action bar when `mapStale` is true. | After deleting a session that fed the current map, a staleness hint is visible in the Sessions view. | open |
| UX-9 | ux-reviewer | insight panel order | P2 | `HeroCard` folds financial impact into its header band rather than a distinct `FinancialCard`, diverging from the documented trust→insight→financial→review order. | Either update the documented order to reflect the intentional merge, or split the dollar figure into its own card below the hero. | The on-screen order matches the documented sequence, or docs are updated to match the merged design. | open |
| UX-10 | ux-reviewer | demo-safe clarity | P2 | The `LiveToggle` and the DEMO-SAFE/LIVE pill are two separate top-bar controls encoding the same state; a presenter could flip to live Gemini accidentally. | Visually couple them (pill = toggle) or confirm when switching to LIVE on protected K-12. | Toggling live mode is a single clearly-labeled control; switching the demo workspace to live is hard to do accidentally. | open |
| UX-11 | ux-reviewer | interview capture | P2 | `InterviewCapture` identity parsing is silent and brittle with no correction affordance; downstream extraction relies on the SPEAKER IDENTITY header. | Let the user edit the captured identity fields inline before saving; soft-warn if `identityComplete` is false at save. | A user who mis-states their name can correct it before saving without re-recording. | open |
| UX-12 | ux-reviewer | accessibility/feedback | P2 | The whole session card opens the transcript while a small checkbox (with `stopPropagation`) handles selection — selection for the headline map flow is easy to miss. | Make selection more discoverable (card body toggles selection with a separate "View transcript" affordance, or a visible "Select for map" area). | A first-time user can select sessions for the map without discovering the small checkbox; selection state is visually obvious. | open |
